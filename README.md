## What:

I want to create an utility module for python daily usage. Keep updating.

## Usage:

Put [common_config.py](common_config.py) and [common_head.py](common_head.py) into your workspace. And then `from common_head import *` at where you want to use it.

The file [try_head_file.py](try_head_file.py) can be used to detect what has been imported.
