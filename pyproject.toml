[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "python_common_head"
version = "0.1.0"
description = "Utility helpers for common Python workflows"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "KuRRe8" }
]

[tool.setuptools]
packages = ["python_common_head"]
